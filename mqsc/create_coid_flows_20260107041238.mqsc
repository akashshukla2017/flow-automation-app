* MQSC Script generated on 20260107041238
DEFINE QLOCAL('QM1') REPLACE

* --- COID 1923456 ---
DEFINE QLOCAL('Q.1923456.COID.IN') REPLACE
DEFINE QLOCAL('Q.1923456.COID.OUT') REPLACE
DEFINE PROCESS('P.1923456.COID') APPLTYPE(UNIX) INITQ('Q.1923456.COID.IN')
DEFINE CHANNEL('CH.1923456.COID') CHLTYPE(SVRCONN) TRPTYPE(TCP) REPLACE

* --- COID 49567 ---
DEFINE QLOCAL('Q.49567.COID.IN') REPLACE
DEFINE QLOCAL('Q.49567.COID.OUT') REPLACE
DEFINE PROCESS('P.49567.COID') APPLTYPE(UNIX) INITQ('Q.49567.COID.IN')
DEFINE CHANNEL('CH.49567.COID') CHLTYPE(SVRCONN) TRPTYPE(TCP) REPLACE

* --- COID 78978 ---
DEFINE QLOCAL('Q.78978.COID.IN') REPLACE
DEFINE QLOCAL('Q.78978.COID.OUT') REPLACE
DEFINE PROCESS('P.78978.COID') APPLTYPE(UNIX) INITQ('Q.78978.COID.IN')
DEFINE CHANNEL('CH.78978.COID') CHLTYPE(SVRCONN) TRPTYPE(TCP) REPLACE
