* ============================================================
* MQSC Script to define COID flows dynamically
* Generated on 20260106205716
* ============================================================

DEFINE QLOCAL('QM1') REPLACE

* --- COID 45 ---
DEFINE QLOCAL('Q.45.COID.IN') REPLACE
DEFINE QLOCAL('Q.45.COID.OUT') REPLACE
DEFINE PROCESS('P.45.COID') APPLTYPE(UNIX) INITQ('Q.45.COID.IN')
DEFINE CHANNEL('CH.45.COID') CHLTYPE(SVRCONN) TRPTYPE(TCP) REPLACE

* --- COID 86 ---
DEFINE QLOCAL('Q.86.COID.IN') REPLACE
DEFINE QLOCAL('Q.86.COID.OUT') REPLACE
DEFINE PROCESS('P.86.COID') APPLTYPE(UNIX) INITQ('Q.86.COID.IN')
DEFINE CHANNEL('CH.86.COID') CHLTYPE(SVRCONN) TRPTYPE(TCP) REPLACE
