* MQSC Script generated on 20260112115230
DEFINE QLOCAL('QM1') REPLACE

* --- COID 4569 ---
DEFINE QLOCAL('Q.4569.COID.IN') REPLACE
DEFINE QLOCAL('Q.4569.COID.OUT') REPLACE
DEFINE PROCESS('P.4569.COID') APPLTYPE(UNIX) INITQ('Q.4569.COID.IN')
DEFINE CHANNEL('CH.4569.COID') CHLTYPE(SVRCONN) TRPTYPE(TCP) REPLACE

* --- COID 2897 ---
DEFINE QLOCAL('Q.2897.COID.IN') REPLACE
DEFINE QLOCAL('Q.2897.COID.OUT') REPLACE
DEFINE PROCESS('P.2897.COID') APPLTYPE(UNIX) INITQ('Q.2897.COID.IN')
DEFINE CHANNEL('CH.2897.COID') CHLTYPE(SVRCONN) TRPTYPE(TCP) REPLACE

* --- COID 9778 ---
DEFINE QLOCAL('Q.9778.COID.IN') REPLACE
DEFINE QLOCAL('Q.9778.COID.OUT') REPLACE
DEFINE PROCESS('P.9778.COID') APPLTYPE(UNIX) INITQ('Q.9778.COID.IN')
DEFINE CHANNEL('CH.9778.COID') CHLTYPE(SVRCONN) TRPTYPE(TCP) REPLACE

* Summary: COIDs included -> [4569, 2897, 9778]
