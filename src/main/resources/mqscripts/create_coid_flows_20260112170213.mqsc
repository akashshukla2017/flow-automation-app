* MQSC Script generated on 20260112170213
DEFINE QLOCAL('QM1') REPLACE

* --- COID 555 ---
DEFINE QLOCAL('Q.555.COID.IN') REPLACE
DEFINE QLOCAL('Q.555.COID.OUT') REPLACE
DEFINE PROCESS('P.555.COID') APPLTYPE(UNIX) INITQ('Q.555.COID.IN')
DEFINE CHANNEL('CH.555.COID') CHLTYPE(SVRCONN) TRPTYPE(TCP) REPLACE

* --- COID 44666 ---
DEFINE QLOCAL('Q.44666.COID.IN') REPLACE
DEFINE QLOCAL('Q.44666.COID.OUT') REPLACE
DEFINE PROCESS('P.44666.COID') APPLTYPE(UNIX) INITQ('Q.44666.COID.IN')
DEFINE CHANNEL('CH.44666.COID') CHLTYPE(SVRCONN) TRPTYPE(TCP) REPLACE
